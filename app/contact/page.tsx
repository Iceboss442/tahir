import type { Metadata } from "next"; import { PageShell } from "../site";
export const metadata: Metadata = { title: "Contact | Tahir Textile", description: "Contact Tahir Textile directly on WhatsApp or call for current fabrics, prices and availability." };
export default function Contact() { return <PageShell page="contact" />; }
