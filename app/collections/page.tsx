import type { Metadata } from "next"; import { PageShell } from "../site";
export const metadata: Metadata = { title: "Collections | Tahir Textile", description: "Explore Tahir Textile's premium Ankara and luxury fabric selections." };
export default function Collections() { return <PageShell page="collections" />; }
