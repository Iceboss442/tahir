import type { Metadata } from "next"; import { PageShell } from "../site";
export const metadata: Metadata = { title: "How to Order | Tahir Textile", description: "A direct WhatsApp ordering process for premium Ankara and luxury fabrics." };
export default function HowToOrder() { return <PageShell page="order" />; }
